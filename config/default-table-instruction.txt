请使用当前提示词模板生成这张表的第一版完整标注。类和所有字段的名称、详细中英文业务语义描述及中英文 aliases 都必须填写；每个字段还必须给出可供人工审核的 AI 标注分析，解释命名、别名、枚举证据、关系语义与不确定性。枚举应先根据原始 JSON 及 RB、WEB、DB 中的常量、配置、赋值和分支确认，再复用当前业务域中语义与取值一致的 enum_ref。isLocalId、isCode、isDisplayName 和 isSemantic 是人工标志，不要判断或修改。
